# Deposit Sample (Pass)

This sample is intentionally structured to pass automated checks:
- source folder present (`src/`)
- build script present (`Makefile`)
- includes `README` and `LICENSE`
# mobilypay
